# Literal `a` pronunciation evaluation

These two clips send the literal normalized word `a` to Jessica with no
dictionary and no local text override. Compare them with the first clip in the
Jessica candidate montage, which used the proposed `a → ay` alias behavior.
