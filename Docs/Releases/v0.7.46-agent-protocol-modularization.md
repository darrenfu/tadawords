# v0.7.46 Agent Protocol Modularization

Version: `0.7.46`  
Build: `2026072503`  
Issue: [#140](https://github.com/darrenfu/tadawords/issues/140)

## Change

- Reduce the always-loaded root `AGENTS.md` from 379 lines and roughly 28 KB
  to a compact authority, safety, and routing document.
- Move the detailed delivery rules into seven focused English modules under
  `Docs/AgentProtocol/`.
- Preserve the existing Simplified Chinese translation in a separate linked
  document rather than duplicating it in the root instruction file.
- Preserve the existing delivery, signing, exact-HEAD, and guarded-merge policy
  semantics without changing product behavior.

## Verification

- Original English protocol sections are normalized and compared with their
  routed modules.
- The root authority and non-negotiable rules are compared with the previous
  root document.
- The complete existing Chinese translation is compared with the new linked
  translation.
- Every relative Markdown link in the root and module directory resolves.
- `git diff --check` passes.

## Packaging

This release changes version/build metadata only because the standing protocol
requires every pull request, including documentation and internal automation
pull requests, to reserve a release identity. It does not change product source,
resources, entitlements, persistence, signing configuration, or runtime
behavior.
